<h2>Make a new post in <%= link_to @blog.title, @blog %>:</h2>

<%= form_for [@blog, @post] do |f| %>
	<label>Enter Post Title:</label>
	<%= f.text_field :title %>
	<label>Enter Post Content:</label>
	<%= f.text_area :content, :size => "50x5" %>
	<%= submit_tag "Submit" %>	
<% end %>
